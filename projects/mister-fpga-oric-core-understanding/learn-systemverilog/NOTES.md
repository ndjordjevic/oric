# Notes

- User wants lessons scoped strictly to **SystemVerilog command/language syntax** as it appears in `Oric.sv` — not the architectural/functional walkthrough (that's already covered by `01a-Oric-sv-understanding.md`). Each lesson should teach the *constructs* (keywords, operators, statement forms) using real lines from the file, not re-explain what the section does at the system level.
- Follows the sequence of `Oric.sv`'s own `★` sections (see `01a-Oric-sv-understanding.md`) as a natural syllabus — each section tends to introduce a new cluster of syntax.
- **Stated goal (2026-07-09):** when looking at the source file, the user wants to understand *every line* from the lessons. Mechanism: `reference/systemverilog-decoder.html` — a card listing every token/operator/literal form that appears anywhere in the studied SystemVerilog/Verilog sources, mapped to meaning + lesson. Invariant (restated 2026-07-12, per-language rather than per-file): if any token appears in **any `.sv`/`.v` file studied so far** (currently just `Oric.sv`; Phase 4 will add more) that isn't on the decoder card, that's a gap to close — extend the card, and add a new lesson only if the construct is genuinely new. Keep the card exhaustive as the studied-file set grows.
- **Coverage verification (2026-07-10, second review pass):** scanned every construct in `core/Oric.sv` against lessons + decoder card. Gaps found and closed: single-statement `always` without `begin/end`, expression width bounds (`[W-1:0]`), non-zero-based ranges (`[27:1]`), comma-separated declarations. Also fixed: all lesson line numbers actually match `annotated/Oric.sv` (925 lines), not pristine `core/Oric.sv` (906 lines) — attributions corrected to `annotated/`.
- **Typography rule (2026-07-10, after user caught inconsistent tagging):** in lesson prose, every *literal language token* (keyword, operator, signal name — anything you'd type in code) gets a `<code>` tag; plain-English concept words (port, signal-as-concept, block) stay untagged. Never mix in one slash-pair (e.g. `<code>module</code>/port` is wrong — either tag both tokens or rephrase with "or"/"and"). Applies to lessons and reference cards alike.
- **Quiz-scope audit (2026-07-10, third pass, user-triggered):** user caught SV Lesson 1 Q2/Q3 testing constructs not taught in that lesson (Q2 needed multi-bit-split concatenation, never shown — only scalar-broadcast was taught; Q3 referenced `if` conditions, not introduced until Lesson 4). Fixed both, then swept every quiz question in both series against its own lesson body. Found one more: SV Lesson 4 Q1's answer relied on "later non-blocking assignment to the same signal wins" — a real, important rule that was never taught, only asserted in the answer. Fixed by teaching the rule in the body (not by watering down the question, since it's genuinely useful and directly explains the code). Remaining 17 lessons checked clean — every question grounded in same-lesson content. Rule going forward: every quiz answer must cite only constructs already taught by that point in the reading order; if an answer needs a rule the prose never stated, teach the rule, don't just narrow the question.
- **Full-coverage audit (2026-07-12, fourth pass):** applied the new AGENTS.md "full-coverage requirement" to every lesson: diffed each code excerpt against its prose so every signal, operator, and literal shown is named or covered by an explicit listed summary, and every elision (`...` or line-number jump) is stated with what's omitted. Lessons 1–9 all gained coverage (L1: USER_OUT/UART trio/clk_sys identities; L2: .ARY line, 1'd1, CROP_SIZE/CROP_OFF/SCALE; L3: CONF_STR elision stated, status-decode excerpt expanded to full 123–132 block; L4: manual_reset_req/tap_autorun_reset_req named, + 1'd1 increment; L5: pll's refclk/outclk_0/outclk_1/locked, hps_io elision stated, sd_buff_din walked — plus two HTML fixes (broken `</em` tag, analogy div closed with `</p>`); L6: both excerpts expanded to full blocks (img_wp if-body, tap_autorun twin lines); L7: .RESET line, line 428's expression decoded; L8: rom/bios_loaded identified, FILE_CACHE_ADDR_WIDTH-1:0 explained, five _active/_addr pairs listed by name; L9: video_mixer placement of .R/.G/.B). L10 checked, no excerpt gaps.
- **Syntax-focus re-polish (2026-07-14, user-triggered):** user re-asserted "lessons focused only on SV syntax." Removed the two genuine architecture insertions that had crept in (both added earlier this same session at the user's request, now reversed): L5's HPS paragraph (what Hard Processor System *is* in the machine) → relocated into `01a §6`, L5 left with a one-line token pointer; L2's "video_freak does aspect-ratio correction" → cut, pointer to `01a §2`. Kept L2's `VGA_DE` driver-chain note (it's a language fact — a top-level port referenced before its driver appears, wired later by `.*`), and cut only the heavy "framework's convention for a preset selector" gloss from L2's `.ARX`/`.ARY` prose, keeping a light one-clause gloss consistent with the untouched sibling ports (`.CROP_SIZE`/`.SCALE`). Keep/cut test used throughout: KEEP one-clause "what a token IS" (required by full-coverage) + how a construct behaves; CUT "what the subsystem does in the machine." L1 deliberately left untouched — its many framework/port mentions are all required port-naming, and it already defers function to 01a explicitly (line 202). Companion pass: `01a` gained a plain-English "what it does" lead per section (tasks paired). Note: 01a now intentionally overlaps the `★` headlines — softens AGENTS.md's one-fact-one-layer rule for that doc; flagged to user.
