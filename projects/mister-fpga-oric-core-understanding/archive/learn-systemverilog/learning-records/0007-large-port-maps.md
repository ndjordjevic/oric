# Learning record — SV Lesson 0007: Reading a large port map

**Date:** 2026-07-14
**Status:** Done.

## Self-check (per MISSION.md's success criteria — can you explain this without the lesson open?)

- [x] Recognizing the `oricatmos` instantiation as the same `module_name instance_name (.port(signal), ...)` shape, just scaled to ~90 ports
- [x] Why matching (or differing) port/signal names either side of `.port(signal)` means nothing by itself
- [x] Bit-slices and a ternary expression written inline directly inside a port connection, with no named `wire` first
- [x] `()` for a deliberately unconnected output, and reading `//` comments inside a port list
- [x] Quiz Q1–Q2 — answer from memory, then check



## Notes / what actually clicked



## Next

- [x] Lesson 0008 — chained ternary & priority encoders