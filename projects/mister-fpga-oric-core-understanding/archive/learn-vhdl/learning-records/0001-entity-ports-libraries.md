# Learning record — VHDL Lesson 0001: Libraries, `ENTITY` & `PORT`

**Date:** 2026-07-16
**Status:** Done. No questions.

## Self-check (per MISSION.md's success criteria — can you explain this without the lesson open?)

- [x] `LIBRARY`/`USE` as VHDL's import statement, and what `std_logic_1164`/`numeric_std` each bring in
- [x] `--` line comments vs. SystemVerilog's `//`
- [x] `ENTITY name IS PORT ( ... ); END;` as VHDL's counterpart to `module ... endmodule` — and why VHDL splits interface (`ENTITY`) from implementation (`ARCHITECTURE`), unlike SV
- [x] `STD_LOGIC` vs. `STD_LOGIC_VECTOR(n DOWNTO 0)`, and `DOWNTO` vs. SV's `[msb:lsb]`
- [x] A default port value via `:=`, and single-quoted (`'0'`, 1 bit) vs. double-quoted (`"00"`, bit vector) literals
- [x] Quiz Q1–Q3 — answer from memory, then check



## Notes / what actually clicked



## Next

- [x] Lesson 0002 — `ARCHITECTURE` & `SIGNAL`